# Weimar Crisis

**CRITICAL: Always use sub-agents for ANY implementation work.** The main conversation must NEVER be blocked by coding, editing, or long operations. The user expects to keep talking (often via voice mode) while work happens in the background. If you catch yourself writing more than 5 lines of code in the main thread, STOP and dispatch a sub-agent instead.
